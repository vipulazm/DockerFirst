services:
  app:
    image: docker-first:1.0
    ports:
      - "8080:8080"
    environment:
        - SERVER_PORT:8080
    volumes:
      - names-volume:/app
volumes:
    names-volume:
      name: vol-first
